{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run main.exe (MSVC)",
      "type": "cppvsdbg",
      "request": "launch",
      "program": "${fileDirname}\\${fileBasenameNoExtension}.exe",
      "args": [],
      "cwd": "${fileDirname}",
      "preLaunchTask": "msvc build active file",
      "console": "integratedTerminal",
      "stopAtEntry": false
    }
  ]
}
