{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run app.exe (MSVC)",
      "type": "cppvsdbg",
      "request": "launch",
      "program": "${workspaceFolder}\\app.exe",
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "Build with MSVC",
      "console": "integratedTerminal"
    }
  ]
}

